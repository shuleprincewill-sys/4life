{
  "name": "camepharm-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @camepharm/backend build && npm run -w @camepharm/web build",
    "dev": "concurrently -k -n backend,web -c blue,green \"npm run -w @camepharm/backend dev\" \"npm run -w @camepharm/web dev\"",
    "lint": "eslint ."
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "eslint": "^9.8.0"
  }
}
